const express = require("express");
const router = express.Router();
const controller = require("@controllers/leaderboard");

// router.post("/", controller.newTransaction);

// router.get("/", controller.mining);

module.exports = router;
